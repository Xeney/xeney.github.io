# 📚 Подготовка к лабораторной работе по ООП  
> 🎯 Цель этого файла — помочь быстро повторить и понять ключевые концепции ООП перед выполнением и защитой лабораторной работы по дисциплине «Объектно-ориентированное программирование» в Самарском университете им. Королёва.

## 📖 Оглавление
- [Основные принципы ООП](#-основные-принципы-ооп-простыми-словами)
- [Ключевые элементы ООП в C#](#-ключевые-элементы-ооп-в-c)
- [Практикум: задачи из лабораторной](#-практикум-решаем-задачи-из-твоей-лабораторной)
- [Работа с исключениями](#-работа-с-исключениями)
- [✅ Чек-лист для защиты лабораторной](#-чек-лист-для-защиты-лабораторной)

---

## 🧠 Основные принципы ООП простыми словами

Объектно-Ориентированное Программирование (ООП) — это подход, при котором программа строится из объектов, как дом из кирпичиков. Каждый объект — это модель из реального мира со своими свойствами и действиями.

**Четыре главных "кита" ООП:**

- **Инкапсуляция**: Объект — это "чёрный ящик". Всё, что ему нужно для работы, спрятано внутри, а снаружи доступны только необходимые кнопки-методы.  
  *Пример:* Чтобы посмотреть фильм на телевизоре, тебе не нужно знать, как устроена его матрица — ты просто используешь пульт.

- **Наследование**: Можно создавать новые объекты на основе существующих, перенимая их свойства и добавляя что-то своё.  
  *Пример:* Класс `Смартфон` может наследовать общие свойства от класса `МобильныйТелефон` (например, возможность звонить), но добавить уникальные, вроде выхода в интернет.

- **Полиморфизм**: Одно действие можно выполнять разными способами в зависимости от объекта.  
  *Пример:* И `ВодительАвтобуса`, и `ВодительТакси` умеют "вести транспортное средство", но делают это по-разному.

- **Абстракция**: Использование объектов, зная **что** они делают, но не вдаваясь в детали **как** они это делают.  
  *Пример:* Ты пользуешься смартфоном, не зная, как работает каждый его чип.

---

## 🛠️ Ключевые элементы ООП в C#

Вот как эти принципы реализуются в коде:

| Элемент       | Простыми словами                              | Пример в C#                     |
|---------------|-----------------------------------------------|----------------------------------|
| **Класс**     | Чертёж или шаблон для создания объектов       | `class Car { ... }`             |
| **Объект**    | Конкретный экземпляр по чертежу               | `Car myCar = new Car();`        |
| **Поле**      | Характеристика объекта (обычно `private`)     | `private string model;`         |
| **Свойство**  | "Контролёр" для безопасного доступа к полю    | `public string Model { get; set; }` |
| **Метод**     | Функция, которую может выполнять объект       | `public void Drive() { ... }`   |
| **Конструктор** | Метод, вызываемый при создании объекта       | `public Car(string modelName) { ... }` |

---

## 💻 Практикум: решаем задачи из твоей лабораторной

### Задача 1: Класс "Прямоугольник"

Реализация с инкапсуляцией, свойствами и конструкторами:

```csharp
/// <summary>
/// Класс для работы с прямоугольниками
/// </summary>
public class Rectangle
{
    private double _length;
    private double _width;
    
    public Rectangle()
    {
        _length = 1.0;
        _width = 1.0;
    }
    
    public Rectangle(double length, double width)
    {
        _length = length;
        _width = width;
    }
    
    public double Length
    {
        get => _length;
        set => _length = value > 0 ? value : _length;
    }
    
    public double Width
    {
        get => _width;
        set => _width = value > 0 ? value : _width;
    }
    
    public double GetArea() => _length * _width;
    public double GetPerimeter() => 2 * (_length + _width);
    public bool IsSquare() => _length == _width;
}
```

**Пример использования:**
```csharp
Rectangle rect1 = new Rectangle();          // 1x1
Rectangle rect2 = new Rectangle(5, 3);      // 5x3

Console.WriteLine($"Площадь: {rect2.GetArea()}");        // 15
Console.WriteLine($"Квадрат: {rect2.IsSquare()}");       // False

rect2.Length = 10;
Console.WriteLine($"Новая площадь: {rect2.GetArea()}");  // 30
```

---

### Задача 5: Статический класс "Калькулятор"

```csharp
/// <summary>
/// Статический класс для математических вычислений
/// </summary>
public static class Calculator
{
    public static long Factorial(int n)
    {
        if (n < 0)
            throw new ArgumentException("Число не может быть отрицательным");
        long result = 1;
        for (int i = 1; i <= n; i++)
            result *= i;
        return result;
    }

    public static double Sqrt(double number)
    {
        if (number < 0)
            throw new ArgumentException("Нельзя извлечь корень из отрицательного числа");
        return Math.Sqrt(number);
    }

    public static double Add(double a, double b) => a + b;
    public static double Subtract(double a, double b) => a - b;
    public static double Multiply(double a, double b) => a * b;
    public static double Divide(double a, double b) => b != 0 ? a / b : throw new DivideByZeroException();
}
```

**Пример использования:**
```csharp
double sum = Calculator.Add(5, 3.5);
long fact = Calculator.Factorial(5); // 120
double sqrt = Calculator.Sqrt(25);   // 5
```

---

## 💡 Работа с исключениями

Безопасное чтение чисел от пользователя:

```csharp
public static double GetValidDouble(string prompt)
{
    while (true)
    {
        Console.Write(prompt);
        string input = Console.ReadLine();

        if (string.IsNullOrEmpty(input))
        {
            Console.ForegroundColor = ConsoleColor.Red;
            Console.WriteLine("Ошибка! Ввод не может быть пустым.");
            Console.ResetColor();
            continue;
        }

        try
        {
            if (double.TryParse(input, out double value))
                return value;
            throw new FormatException("Некорректный формат числа.");
        }
        catch (FormatException ex)
        {
            Console.ForegroundColor = ConsoleColor.Red;
            Console.WriteLine($"Ошибка! {ex.Message}");
            Console.ResetColor();
        }
    }
}
```

---

## ✅ Чек-лист для защиты лабораторной

- [ ] Могу объяснить 4 принципа ООП своими словами  
- [ ] Могу показать в коде: класс, объект, поле, свойство, метод, конструктор  
- [ ] Понимаю разницу между `private` полем и `public` свойством  
- [ ] Знаю, зачем нужны статические классы и методы  
- [ ] Умею обрабатывать исключения (`try`/`catch`, `ArgumentException`, `DivideByZeroException`)  
- [ ] Могу привести примеры из своего кода (прямоугольник, калькулятор)  
- [ ] Могу показать, как работает инкапсуляция и проверка ввода